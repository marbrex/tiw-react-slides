import * as React from 'react'

interface Props {
  slides: Slide[]
}

const AppToolbar: React.FC<Props> = ({ slides }): JSX.Element => {
  return <div className='AppToolbar'></div>
}

export default AppToolbar
